# Dokcer + Python3 + Mysql + Scrapy

## Getting Started
I created this project only to do some test, so do not copy this code.


## Installation
- Install docker
- cd to_project_directory
- start app 
```bash
docker-compose up --build
```